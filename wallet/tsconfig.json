{
  "compilerOptions": {
    /* Visit https://aka.ms/tsconfig to read more about this file */

    /* Projects */
    "incremental": true,                              /* Enable incremental compilation to speed up builds. */

    /* Language and Environment */
    "target": "es2020",                               /* Set the JavaScript language version for emitted JavaScript. */
    "lib": ["es2020", "dom"],                         /* Specify library files for the target runtime environment. */
    "jsx": "react-jsx",                               /* Enable the new JSX transform for React. */
    "moduleDetection": "auto",                        /* Automatically detect module type (ESM or CommonJS). */

    /* Modules */
    "module": "esnext",                               /* Generate modern ES module code. */
    "rootDir": "src",                                 /* Specify the root folder for source files. */
    "outDir": "dist",                                 /* Output folder for compiled JavaScript files. */
    "baseUrl": ".",                                   /* Base directory to resolve non-relative module names. */
    "paths": {                                        /* Import alias mapping. */
      "@/components/*": ["src/components/*"],
      "@/utils/*": ["src/utils/*"],
      "@/styles/*": ["src/styles/*"]
    },
    "esModuleInterop": true,                          /* Enable compatibility with CommonJS modules. */
    "forceConsistentCasingInFileNames": true,         /* Ensure consistent casing in file imports. */
    "resolveJsonModule": true,                        /* Allow importing .json files. */
    "moduleResolution": "node",                       /* Use Node.js module resolution strategy. */
    "allowSyntheticDefaultImports": true,            /* Allow default imports even if the module doesn't have one. */

    /* JavaScript Support */
    "allowJs": true,                                  /* Allow JavaScript files in the project. */
    "checkJs": false,                                 /* Disable type checking for JavaScript files. */

    /* Emit */
    "declaration": true,                              /* Generate .d.ts files for TypeScript files. */
    "declarationMap": true,                           /* Create sourcemaps for .d.ts files. */
    "sourceMap": true,                                /* Generate sourcemap files for debugging. */
    "inlineSources": true,                            /* Include the source code in the sourcemaps. */

    /* Interop Constraints */
    "strict": true,                                   /* Enable all strict type-checking options. */
    "strictNullChecks": true,                         /* Check for `null` and `undefined` explicitly. */
    "noImplicitAny": true,                            /* Report errors on expressions with an implied `any` type. */
    "noImplicitThis": true,                           /* Raise error when `this` has an implicit `any` type. */
    "useUnknownInCatchVariables": true,               /* Default catch variables to `unknown` instead of `any`. */

    /* Type Checking */
    "skipLibCheck": true,                             /* Skip type checking of declaration files. */
    "noUnusedLocals": true,                           /* Report errors on unused local variables. */
    "noUnusedParameters": true,                       /* Report errors on unused function parameters. */
    "noImplicitReturns": true,                        /* Ensure all code paths in a function return a value. */
    "noFallthroughCasesInSwitch": true,               /* Report errors for fallthrough cases in switch statements. */
    "exactOptionalPropertyTypes": true,               /* Interpret optional properties strictly as written. */

    /* Completeness */
    "isolatedModules": true                           /* Ensure each file can be safely transpiled without relying on imports. */
  },
  "include": ["src/**/*"],                             /* Include all TypeScript and JavaScript files in the src folder. */
  "exclude": ["node_modules", "dist"]                  /* Exclude unnecessary files and folders. */
}
